以下是**優化後可直接貼給 Codex 的版本**。我把它改成「工程實作指令」，補上資料邊界、離線快取、單位換算、測試條件、AI fallback，以及避免 Codex 亂把商業邏輯塞進 View 的要求。

農業部開放資料目前有「農產品交易行情」資料集，欄位包含交易日期、作物代號、作物名稱、市場代號、市場名稱、上價、中價、下價、平均價、交易量等，更新頻率標示為每日；農業資料開放平臺也有 API 服務專區可介接。實作時請把 API client 做成 protocol-first，避免把端點與欄位轉換寫死在 UI。

---

## 給 Codex 的優化版 Prompt

```
你是資深 iOS / SwiftUI / SwiftData 工程師。請在既有 RestaurantOps App 中實作 Phase 2 功能：「RestaurantOps Purchasing：餐廳進貨價格 AI 比價 + 議價助理」。重要策略：- 不要建立獨立 App。- 將本功能併入既有 RestaurantOps 架構。- 功能定位是幫餐廳老闆：  1. 建立常用食材清單  2. 匯入或下載農產品交易行情  3. 輸入供應商報價  4. 比較「供應商報價 vs 市場平均價」  5. 產生可複製到 LINE / 訊息的議價話術請優先完成 deterministic core，再做 AI 議價訊息。AI 不可以負責計算價格、價差、排名或均價；這些都必須由 deterministic service 完成。AI 只能根據 deterministic result 產生文字建議。AI 不可用時，App 仍必須完整顯示價差報表。技術假設：- App 使用 SwiftUI + SwiftData。- 既有 RestaurantOps 已有 restaurantID 的概念。- 請新增 Purchasing feature folder，不要污染其他模組。- 請使用 MVVM 或既有專案架構風格。- 所有服務需 protocol-first，方便 mock、測試與未來替換 API。
```

---

## 建議目錄結構

```
RestaurantOps/  Features/    Purchasing/      Models/        Ingredient.swift        MarketPriceRecord.swift        Supplier.swift        SupplierQuote.swift        IngredientMarketMapping.swift        MarketPriceFetchLog.swift      Services/        AgriPriceAPIClient.swift        MockAgriPriceAPIClient.swift        IngredientMarketMatcher.swift        PriceComparisonService.swift        UnitConversionService.swift        NegotiationDraftAI.swift        PurchasingCacheService.swift      ViewModels/        IngredientListViewModel.swift        IngredientDetailViewModel.swift        MarketPriceTrendViewModel.swift        SupplierListViewModel.swift        SupplierQuoteEntryViewModel.swift        PriceComparisonDashboardViewModel.swift        NegotiationAssistantViewModel.swift      Views/        IngredientListView.swift        IngredientDetailView.swift        MarketPriceTrendView.swift        SupplierListView.swift        SupplierQuoteEntryView.swift        PriceComparisonDashboardView.swift        NegotiationAssistantView.swift      Resources/        ingredient_synonyms_zh_TW.json        agri_price_mock.json  Tests/    PurchasingTests/      AgriPriceAPIClientTests.swift      IngredientMarketMatcherTests.swift      PriceComparisonServiceTests.swift      UnitConversionServiceTests.swift      NegotiationDraftAITests.swift
```

---

## Data Model 改進要求

請以原始資料模型為基礎，但補強以下欄位與關聯。

### 1. Ingredient

```
@Model final class Ingredient {    var id: UUID    var restaurantID: UUID    var name: String    var category: IngredientCategory    var unit: UnitType    var defaultWasteRate: Decimal    // 使用者確認後的市場品項對應    var linkedMarketItemCode: String?    var linkedMarketItemName: String?    var createdAt: Date    var updatedAt: Date}
```

要求：

- `defaultWasteRate` 預設為 0。
- `unit` 必須可轉換成標準比較單位。
- 不要讓 AI 自動修改 `linkedMarketItemCode`；AI 只能提出候選，使用者必須確認。

---

### 2. MarketPriceRecord

原 model 缺少 `marketCode` 與 `categoryCode`，請補上，否則之後難以做精準查詢與去重。

```
@Model final class MarketPriceRecord {    var id: UUID    var categoryCode: String?    var itemCode: String    var itemName: String    var marketCode: String?    var marketName: String    var tradeDate: Date    var highPrice: Decimal?    var midPrice: Decimal?    var lowPrice: Decimal?    var averagePrice: Decimal    var volume: Decimal?    var unit: String    var sourceName: String    var fetchedAt: Date    // 用於避免重複匯入    var sourceKey: String}
```

`sourceKey` 建議格式：

```
sourceName|tradeDate|marketCode|itemCode|unit
```

要求：

- 匯入行情時，若 `sourceKey` 已存在，請更新既有資料，不要重複新增。
- `averagePrice` 必須為 Decimal，不可用 Double 做金額計算。
- API DTO 可以用 String 接，再轉 Decimal。

---

### 3. Supplier

```
@Model final class Supplier {    var id: UUID    var restaurantID: UUID    var name: String    var phone: String?    var lineID: String?    var note: String?    var createdAt: Date    var updatedAt: Date}
```

要求：

- 同一餐廳下 supplier name 不強制唯一，但 UI 可提示疑似重複。
- 供應商列表需可搜尋。

---

### 4. SupplierQuote

```
@Model final class SupplierQuote {    var id: UUID    var supplierID: UUID    var ingredientID: UUID    var quoteDate: Date    var quotedPrice: Decimal    var unit: UnitType    var minOrderQuantity: Decimal?    var note: String?    var createdAt: Date    var updatedAt: Date}
```

要求：

- 不要把價差百分比存進 model，價差應由 `PriceComparisonService` 即時計算。
- 若要快取計算結果，請另外建 read model，不要污染 quote 原始資料。

---

### 5. 新增 IngredientMarketMapping

```
@Model final class IngredientMarketMapping {    var id: UUID    var restaurantID: UUID    var ingredientID: UUID    var marketItemCode: String    var marketItemName: String    var confidence: Decimal    var source: MappingSource // exact, synonym, fuzzy, aiSuggested, userConfirmed    var isUserConfirmed: Bool    var confirmedAt: Date?    var createdAt: Date    var updatedAt: Date}
```

用途：

- 保存「餐廳食材名稱」到「市場作物名稱」的對應。
- MVP 可先用 synonym table。
- AI 只能產生候選，不得自動確認。

---

### 6. 新增 MarketPriceFetchLog

```
@Model final class MarketPriceFetchLog {    var id: UUID    var sourceName: String    var startedAt: Date    var finishedAt: Date?    var status: FetchStatus    var recordCount: Int    var errorMessage: String?}
```

用途：

- 顯示最近一次行情更新時間。
- 無網路時，讓 UI 顯示「使用上次快取行情」。

---

## Enum 要求

請建立清楚、可 Codable 的 enum。

```
enum UnitType: String, Codable, CaseIterable {    case kg    case g    case liter    case ml    case piece    case box    case catty // 台斤，若台灣餐廳常用可支援}enum IngredientCategory: String, Codable, CaseIterable {    case vegetable    case fruit    case meat    case seafood    case dairy    case dryGoods    case seasoning    case beverage    case other}enum MappingSource: String, Codable {    case exact    case synonym    case fuzzy    case aiSuggested    case userConfirmed}enum FetchStatus: String, Codable {    case success    case failed    case partial}
```

---

## Service 1：AgriPriceAPIClient

請實作 protocol-first。

```
protocol AgriPriceAPIClient {    func fetchMarketPrices(        from startDate: Date,        to endDate: Date,        itemName: String?,        itemCode: String?,        marketName: String?,        marketCode: String?    ) async throws -> [MarketPriceDTO]}
```

要求：

- 建立 `LiveAgriPriceAPIClient` 與 `MockAgriPriceAPIClient`。
- `MockAgriPriceAPIClient` 從 `agri_price_mock.json` 讀取。
- Live client 不要直接回傳 SwiftData model，請先回傳 DTO。
- DTO mapper 負責處理：
    - 中文欄位名稱
    - Decimal parsing
    - 日期 parsing
    - 缺漏欄位
    - 空字串
- 支援 date range、item name、item code、market name、market code filter。
- 若 API 本身不支援所有 filter，請 client 下載後在本地 filter，但要封裝在 client 或 repository，不要放在 View。

DTO 範例：

```
struct MarketPriceDTO: Codable {    let tradeDateRaw: String    let categoryCode: String?    let itemCode: String?    let itemName: String    let marketCode: String?    let marketName: String    let highPriceRaw: String?    let midPriceRaw: String?    let lowPriceRaw: String?    let averagePriceRaw: String    let volumeRaw: String?}
```

請建立 mapper：

```
struct MarketPriceMapper {    func map(_ dto: MarketPriceDTO, sourceName: String, fetchedAt: Date) throws -> MarketPriceRecordInput}
```

注意：

- API 日期格式可能不是 ISO 8601，請做容錯 parser。
- 價格為元/公斤時，unit 設為 `kg`。
- 若 average price 無法解析，該筆資料應跳過並記錄 error，不要 crash。

---

## Service 2：IngredientMarketMatcher

```
protocol IngredientMarketMatcher {    func candidates(        for ingredientName: String,        availableMarketItems: [MarketItem]    ) async -> [IngredientMarketCandidate]}
```

MVP matching priority：

```
1. Exact match2. Synonym table match3. Normalized contains match4. Simple fuzzy match5. AI candidate suggestion, optional
```

要求：

- `ingredient_synonyms_zh_TW.json` 放在 Resources。
- Synonym table 範例：

```
{  "高麗菜": ["甘藍", "包心白菜"],  "小黃瓜": ["胡瓜"],  "番茄": ["蕃茄", "小番茄"],  "青蔥": ["蔥", "珠蔥"],  "馬鈴薯": ["洋芋"]}
```

候選結果：

```
struct IngredientMarketCandidate: Identifiable, Equatable {    let id: UUID    let itemCode: String    let itemName: String    let confidence: Decimal    let source: MappingSource    let reason: String}
```

重要限制：

- AI candidate 不得直接寫入 `Ingredient.linkedMarketItemCode`。
- UI 必須要求使用者點選「確認對應」。
- 使用者確認後才建立或更新 `IngredientMarketMapping`。

---

## Service 3：UnitConversionService

新增單位換算服務，所有比價前必須先標準化單位。

```
protocol UnitConversionService {    func normalizePrice(        price: Decimal,        from unit: UnitType,        ingredient: Ingredient    ) throws -> NormalizedPrice}
```

```
struct NormalizedPrice {    let price: Decimal    let unit: UnitType}
```

MVP 規則：

- kg → kg：不變
- g → kg：price * 1000
- 台斤 catty → kg：price / 0.6
- liter / ml：只在液體食材可比，ml → liter
- piece / box：不可直接和 kg 比，除非該 ingredient 有 conversion note 或 pack size 設定
- 無法換算時，UI 顯示「單位不同，需手動確認」，不要硬算

請避免：

- 不可用 Double 計算價格。
- 不可把 piece 與 kg 直接比較。
- 不可把 box 與 kg 直接比較。

---

## Service 4：PriceComparisonService

```
protocol PriceComparisonService {    func compare(        ingredient: Ingredient,        supplierQuotes: [SupplierQuote],        marketRecords: [MarketPriceRecord],        asOf date: Date    ) throws -> PriceComparisonResult}
```

輸出：

```
struct PriceComparisonResult {    let ingredientID: UUID    let ingredientName: String    let marketAverage7Days: Decimal?    let marketAverage30Days: Decimal?    let latestMarketAverage: Decimal?    let supplierComparisons: [SupplierPriceComparison]    let generatedAt: Date    let warnings: [String]}struct SupplierPriceComparison {    let supplierID: UUID    let supplierName: String    let quoteID: UUID    let quoteDate: Date    let quotedPrice: Decimal    let normalizedQuotedPrice: Decimal?    let marketReferencePrice: Decimal?    let differenceAmount: Decimal?    let differencePercent: Decimal?    let rank: Int?    let status: PriceComparisonStatus}enum PriceComparisonStatus {    case cheaperThanMarket    case nearMarket    case higherThanMarket    case missingMarketData    case unitMismatch}
```

計算規則：

- 價差金額 = normalized supplier price - market reference price
- 價差百分比 = 價差金額 / market reference price * 100
- `nearMarket` 預設區間：-5% 到 +5%
- `higherThanMarket`：大於 +5%
- `cheaperThanMarket`：小於 -5%
- 7 日與 30 日均價：
    - 優先用 volume-weighted average
    - 若 volume 缺漏，退回 arithmetic average
- 供應商排名：
    - 只排名可比較且單位一致的報價
    - 價格低者 rank 較前
    - 單位不一致或缺市場資料者 rank = nil

重要：

- 這個 service 必須是 pure deterministic service。
- 請寫完整 unit tests。
- ViewModel 不可自行計算價差百分比。

---

## Service 5：NegotiationDraftAI

```
protocol NegotiationDraftAI {    func generateDraft(context: NegotiationContext) async throws -> NegotiationDraft}
```

```
struct NegotiationContext {    let restaurantName: String?    let supplierName: String    let ingredientName: String    let quotedPrice: Decimal    let marketAverage7Days: Decimal?    let marketAverage30Days: Decimal?    let differencePercent: Decimal?    let recentTrendSummary: String?    let tone: NegotiationTone}enum NegotiationTone: String, Codable, CaseIterable {    case polite    case firm    case friendly    case longTermPartner}struct NegotiationDraft {    let message: String    let fallbackUsed: Bool}
```

AI prompt 要求：

- 使用繁體中文。
- 語氣禮貌但有力。
- 不要威脅供應商。
- 不要捏造不存在的市場資料。
- 若市場資料不足，請明確說「目前參考資料有限」。
- 文字長度適合貼到 LINE，約 80～180 字。
- 不要輸出 Markdown。
- 不要輸出表格。
- 不要自稱 AI。

AI fallback：

- 若 AI API unavailable，使用 deterministic template。
- fallback template 範例：

```
您好，想跟您確認一下這次 {ingredientName} 的報價。目前我們看到近期市場均價大約是 {marketPrice}，這次報價約高出 {differencePercent}%。我們一直希望穩定跟您配合，也想請您幫忙看看這批價格是否還有調整空間，謝謝。
```

---

## View 實作要求

### 1. IngredientListView

功能：

- 顯示餐廳常用食材
- 新增 / 編輯 / 刪除食材
- 顯示是否已連結市場品項
- 搜尋食材
- 點擊進入 IngredientDetailView

UI 狀態：

- Empty state：「尚未建立常用食材」
- 未連結市場品項時顯示 badge：「尚未對應行情」
- 已連結時顯示市場作物名稱

---

### 2. IngredientDetailView

功能：

- 顯示食材基本資料
- 顯示目前市場品項對應
- 可重新執行 matcher
- 顯示候選市場品項
- 使用者確認後儲存 mapping

重要：

- AI 候選需標示「AI 建議，請確認」
- 不可自動套用 AI 候選

---

### 3. MarketPriceTrendView

功能：

- 顯示該食材近 7 / 30 日行情
- 顯示最高價、中價、最低價、平均價
- 顯示資料來源與最後更新時間
- 無資料時顯示提示

注意：

- 圖表可用 Swift Charts。
- 若沒有 Swift Charts，先用列表顯示。
- 不要在 Chart 裡計算均價；請由 ViewModel 呼叫 service。

---

### 4. SupplierListView

功能：

- 新增 / 編輯 / 刪除供應商
- 搜尋供應商
- 顯示電話、LINE ID、備註
- 進入供應商報價紀錄

---

### 5. SupplierQuoteEntryView

功能：

- 選擇供應商
- 選擇食材
- 輸入報價
- 選擇單位
- 輸入最低訂購量
- 備註
- 儲存 quote

驗證：

- 價格必須 > 0
- quoteDate 不可為未來日期
- supplierID 與 ingredientID 必須存在
- 單位若與 ingredient default unit 不同，顯示提醒但允許儲存

---

### 6. PriceComparisonDashboardView

功能：

- 選擇食材
- 顯示市場最新均價、7 日均價、30 日均價
- 顯示各供應商報價比較
- 顯示價差金額與百分比
- 顯示供應商價格排名
- 支援多供應商比較

顯示規則：

- 高於市場 +5% 以上：標示「偏高」
- ±5% 內：標示「接近市場」
- 低於 -5%：標示「低於市場」
- 無市場資料：標示「缺少行情」
- 單位不可比較：標示「單位需確認」

---

### 7. NegotiationAssistantView

功能：

- 從某一筆供應商比較結果進入
- 顯示 deterministic comparison summary
- 選擇語氣
- 產生議價訊息
- 可編輯訊息
- 可複製訊息
- 可分享至系統 share sheet

要求：

- 產生前先顯示資料依據
- AI 不可用時顯示 fallback template
- 複製成功後顯示 toast 或 alert
- 不要要求使用者一定要登入 AI 才能看到價差報表

---

## Offline / Cache 要求

請新增 `PurchasingCacheService` 或 repository 層。

要求：

- 行情下載成功後存入 SwiftData。
- App 無網路時，使用最近一次成功快取。
- UI 顯示：
    - 「使用快取行情」
    - 「最後更新：yyyy/MM/dd HH:mm」
- 若完全沒有快取，顯示：
    - 「目前沒有行情資料，請稍後連線更新」
- API failure 不可讓整個 Purchasing 模組 crash。
- partial import 時需記錄成功筆數與錯誤筆數。

---

## 測試要求

請新增 unit tests，至少包含以下案例。

### PriceComparisonServiceTests

1. 供應商報價 120，市場均價 100，價差 = +20%，status = higherThanMarket。
2. 供應商報價 96，市場均價 100，價差 = -4%，status = nearMarket。
3. 供應商報價 85，市場均價 100，價差 = -15%，status = cheaperThanMarket。
4. 缺少市場資料時，status = missingMarketData。
5. piece 與 kg 不可比較，status = unitMismatch。
6. 多供應商報價時，依 normalized price 排名。
7. 7 日均價與 30 日均價需正確。
8. volume 存在時使用 volume-weighted average。

### UnitConversionServiceTests

1. kg → kg 不變。
2. g 單價轉 kg 單價。
3. 台斤轉 kg。
4. piece 無 pack size 時不可轉 kg。
5. box 無 conversion rule 時不可轉 kg。

### IngredientMarketMatcherTests

1. exact match。
2. synonym match。
3. contains match。
4. fuzzy match。
5. AI candidate 不得自動 confirmed。
6. 使用者確認後才建立 IngredientMarketMapping。

### AgriPriceAPIClientTests

1. mock JSON 可正確 decode。
2. 中文欄位可正確 map。
3. 空字串價格不 crash。
4. averagePrice 缺漏時該筆跳過。
5. 重複 sourceKey 不重複新增。
6. date range filter 正確。

### NegotiationDraftAITests

1. AI success 回傳 message。
2. AI failure 使用 fallback template。
3. fallback message 不可捏造市場資料。
4. message 可複製使用，不含 Markdown 表格。

---

## 驗收標準

請確保以下全部通過：

1. 無網路時顯示最近一次行情快取。
2. 同一食材可比較多個供應商。
3. 價差、均價、排名全部由 deterministic service 計算。
4. AI 產生的議價訊息可複製到 LINE / 訊息。
5. AI 不可用時，仍顯示價差報表。
6. AI 不得自動確認食材與市場作物的對應。
7. 單位不可比較時，不得硬算。
8. SwiftData model 不直接依賴 API DTO。
9. View 不直接呼叫 API client，必須透過 ViewModel / Service / Repository。
10. 所有價格計算使用 Decimal。
11. Mock JSON 可在無網路環境下跑 demo。
12. Unit tests 覆蓋核心服務。

---

## 請優先實作順序

Phase A：資料與服務核心

1. 建立 enums、models、DTO、mapper。
2. 建立 `AgriPriceAPIClient` protocol。
3. 建立 `MockAgriPriceAPIClient`。
4. 建立 `UnitConversionService`。
5. 建立 `PriceComparisonService`。
6. 寫 unit tests。

Phase B：食材與行情對應

1. 建立 synonym table。
2. 實作 `IngredientMarketMatcher`。
3. 實作使用者確認 mapping 流程。
4. 寫 matcher tests。

Phase C：供應商報價

1. Supplier CRUD。
2. SupplierQuote entry。
3. 多供應商比較 dashboard。

Phase D：行情趨勢與快取

1. MarketPrice import。
2. 去重與更新。
3. Fetch log。
4. Offline cache UI。
5. Trend view。

Phase E：AI 議價助理

1. `NegotiationDraftAI` protocol。
2. fallback template。
3. AI provider adapter。
4. 可編輯、複製、分享訊息。
5. AI failure tests。

請完成後輸出：

- 變更檔案列表
- 主要新增型別
- 測試覆蓋項目
- 任何尚未完成或需要人工設定的項目

```
---## 另外建議你把原規格補成這 5 個產品決策這些是 Codex 最容易猜錯、建議先固定下來的地方：1. **市場平均價定義**     MVP 建議：若有指定市場，用該市場；若沒有指定市場，用同一作物所有市場的 volume-weighted average，缺 volume 時才用 arithmetic average。2. **單位比較規則**     kg/g/台斤可換算；piece/box 不可硬比，除非之後新增包裝規格。3. **AI 權限邊界**     AI 只能「寫話術」與「建議候選品項」，不能決定 mapping，不能算價格，不能修改資料。4. **行情資料可信度顯示**     每個 dashboard 都顯示「資料來源、最後更新時間、是否快取」。5. **MVP 先不要做自動議價紀錄管理**     先做到「產生、編輯、複製、分享」。之後再做 supplier negotiation history。::contentReference[oaicite:1]{index=1}
```

整理成一份可直接交給 Codex 的完整任務包

我會把規格整理成 Codex 可直接執行的完整任務包，涵蓋背景、架構、檔案結構、資料模型、服務、畫面、測試、驗收與交付輸出。

已思考 3m 15s

以下是一份**可直接整段貼給 Codex 的完整任務包**。

我把內容整理成 Codex 比較容易執行的格式：明確背景、邊界、資料模型、服務職責、畫面、測試、驗收標準與交付回報格式。Codex 官方文件也建議把任務交代成「像交給隊友一樣，有清楚上下文與完成定義」，而 Codex 本身可讀取、修改並執行程式碼，因此這份任務包特別加入 validation 與 final report 要求。  
本功能的外部行情資料可對接農業部「農產品交易行情」資料集，該資料集提供 API，欄位包含交易日期、作物代號、作物名稱、市場代號、市場名稱、上價、中價、下價、平均價、交易量等，且標示為每日更新；農業資料開放平臺也有交易行情 API 專區。

---

```
# Codex 任務包：RestaurantOps Purchasing Phase 2# 餐廳進貨價格 AI 比價 + 議價助理你是資深 iOS / SwiftUI / SwiftData 工程師。請在既有 RestaurantOps App 中實作 Phase 2 功能：「RestaurantOps Purchasing：餐廳進貨價格 AI 比價 + 議價助理」。請直接修改現有 repository，不要建立獨立 App。請先閱讀既有專案結構、資料層、navigation、SwiftData setup、dependency injection 或 service pattern，再依照既有風格整合。若既有專案架構與本任務包有衝突，請優先維持既有專案一致性，但不可違反本任務包的核心驗收標準。---## 0. 任務背景產品代號：RestaurantOps Purchasing原編號：53產品策略：併入 RestaurantOps，不獨立開 App。產品定位：幫餐廳老闆掌握食材市場行情，對照供應商報價，產生可複製到 LINE / 訊息的議價建議。MVP 目標：1. 建立餐廳常用食材清單。2. 下載 / 匯入農產品交易行情。3. 使用者輸入供應商報價。4. App 顯示「供應商報價 vs 市場平均價」。5. AI 產生禮貌但有力的議價話術。---## 1. 核心原則請嚴格遵守以下原則：1. 不要建立獨立 App。2. 將本功能放入既有 RestaurantOps App。3. 所有價格、均價、價差、排名必須由 deterministic service 計算。4. AI 不可以計算價格、均價、價差或供應商排名。5. AI 只能根據 deterministic result 產生議價文字。6. AI 不可用時，App 仍必須完整顯示價差報表。7. AI 不得自動確認「餐廳食材名稱」與「市場作物名稱」的對應。8. 使用者必須手動確認市場品項 mapping。9. 所有價格計算必須使用 Decimal，不可用 Double。10. View 不可直接呼叫 API client。11. ViewModel 不可自行計算價差百分比。12. SwiftData model 不可直接依賴 API DTO。13. 單位不可比較時，不得硬算。14. 無網路時必須顯示最近一次行情快取。15. Mock JSON 必須可在無網路環境下 demo。---## 2. 建議分支與 PR請建立或使用 feature branch：feature/restaurantops-purchasing-phase-2PR title 建議：Implement RestaurantOps Purchasing Phase 2 price comparison and negotiation assistantPR description 請包含：- 新增功能摘要- 新增資料模型- 新增服務- 新增畫面- 測試覆蓋項目- 尚未完成或需要人工設定的項目---## 3. 建議目錄結構請依照既有專案風格調整，但建議新增以下結構：RestaurantOps/  Features/    Purchasing/      Models/        Ingredient.swift        MarketPriceRecord.swift        Supplier.swift        SupplierQuote.swift        IngredientMarketMapping.swift        MarketPriceFetchLog.swift        PurchasingEnums.swift      DTOs/        MarketPriceDTO.swift        MarketPriceMapper.swift      Services/        AgriPriceAPIClient.swift        LiveAgriPriceAPIClient.swift        MockAgriPriceAPIClient.swift        IngredientMarketMatcher.swift        SynonymIngredientMarketMatcher.swift        PriceComparisonService.swift        DefaultPriceComparisonService.swift        UnitConversionService.swift        DefaultUnitConversionService.swift        NegotiationDraftAI.swift        DefaultNegotiationDraftAI.swift        TemplateNegotiationDraftAI.swift        PurchasingCacheService.swift        PurchasingRepository.swift      ViewModels/        IngredientListViewModel.swift        IngredientDetailViewModel.swift        MarketPriceTrendViewModel.swift        SupplierListViewModel.swift        SupplierQuoteEntryViewModel.swift        PriceComparisonDashboardViewModel.swift        NegotiationAssistantViewModel.swift      Views/        IngredientListView.swift        IngredientDetailView.swift        MarketPriceTrendView.swift        SupplierListView.swift        SupplierQuoteEntryView.swift        PriceComparisonDashboardView.swift        NegotiationAssistantView.swift      Resources/        ingredient_synonyms_zh_TW.json        agri_price_mock.jsonTests/  PurchasingTests/    AgriPriceAPIClientTests.swift    MarketPriceMapperTests.swift    IngredientMarketMatcherTests.swift    PriceComparisonServiceTests.swift    UnitConversionServiceTests.swift    NegotiationDraftAITests.swift    PurchasingCacheServiceTests.swift若既有專案已有 Tests target，請整合到既有 target。---## 4. Data Models請使用 SwiftData `@Model`。若既有專案已有 BaseModel、Auditable、RestaurantScoped pattern，請沿用。### 4.1 Ingredient請建立或擴充：```swift@Model final class Ingredient {    var id: UUID    var restaurantID: UUID    var name: String    var category: IngredientCategory    var unit: UnitType    var defaultWasteRate: Decimal    var linkedMarketItemCode: String?    var linkedMarketItemName: String?    var createdAt: Date    var updatedAt: Date    init(        id: UUID = UUID(),        restaurantID: UUID,        name: String,        category: IngredientCategory,        unit: UnitType,        defaultWasteRate: Decimal = 0,        linkedMarketItemCode: String? = nil,        linkedMarketItemName: String? = nil,        createdAt: Date = Date(),        updatedAt: Date = Date()    ) {        self.id = id        self.restaurantID = restaurantID        self.name = name        self.category = category        self.unit = unit        self.defaultWasteRate = defaultWasteRate        self.linkedMarketItemCode = linkedMarketItemCode        self.linkedMarketItemName = linkedMarketItemName        self.createdAt = createdAt        self.updatedAt = updatedAt    }}
```

要求：

- defaultWasteRate 預設為 0。
- linkedMarketItemCode / linkedMarketItemName 只能在使用者確認後更新。
- AI candidate 不得直接更新這兩個欄位。

---

### 4.2 MarketPriceRecord

```
@Model final class MarketPriceRecord {    var id: UUID    var categoryCode: String?    var itemCode: String    var itemName: String    var marketCode: String?    var marketName: String    var tradeDate: Date    var highPrice: Decimal?    var midPrice: Decimal?    var lowPrice: Decimal?    var averagePrice: Decimal    var volume: Decimal?    var unit: String    var sourceName: String    var fetchedAt: Date    var sourceKey: String    init(        id: UUID = UUID(),        categoryCode: String? = nil,        itemCode: String,        itemName: String,        marketCode: String? = nil,        marketName: String,        tradeDate: Date,        highPrice: Decimal? = nil,        midPrice: Decimal? = nil,        lowPrice: Decimal? = nil,        averagePrice: Decimal,        volume: Decimal? = nil,        unit: String = "kg",        sourceName: String,        fetchedAt: Date = Date(),        sourceKey: String    ) {        self.id = id        self.categoryCode = categoryCode        self.itemCode = itemCode        self.itemName = itemName        self.marketCode = marketCode        self.marketName = marketName        self.tradeDate = tradeDate        self.highPrice = highPrice        self.midPrice = midPrice        self.lowPrice = lowPrice        self.averagePrice = averagePrice        self.volume = volume        self.unit = unit        self.sourceName = sourceName        self.fetchedAt = fetchedAt        self.sourceKey = sourceKey    }}
```

sourceKey 建議格式：

sourceName|tradeDate|marketCode|itemCode|unit

要求：

- 匯入行情時，若 sourceKey 已存在，請更新既有資料，不要重複新增。
- averagePrice 必須使用 Decimal。
- API DTO 可先用 String 接，再由 mapper 轉 Decimal。
- averagePrice 無法解析時，該筆資料應跳過並記錄 error，不要 crash。

---

### 4.3 Supplier

```
@Model final class Supplier {    var id: UUID    var restaurantID: UUID    var name: String    var phone: String?    var lineID: String?    var note: String?    var createdAt: Date    var updatedAt: Date    init(        id: UUID = UUID(),        restaurantID: UUID,        name: String,        phone: String? = nil,        lineID: String? = nil,        note: String? = nil,        createdAt: Date = Date(),        updatedAt: Date = Date()    ) {        self.id = id        self.restaurantID = restaurantID        self.name = name        self.phone = phone        self.lineID = lineID        self.note = note        self.createdAt = createdAt        self.updatedAt = updatedAt    }}
```

要求：

- 同一餐廳下 supplier name 不強制唯一。
- UI 可提示疑似重複。
- 供應商列表需支援搜尋。

---

### 4.4 SupplierQuote

```
@Model final class SupplierQuote {    var id: UUID    var supplierID: UUID    var ingredientID: UUID    var quoteDate: Date    var quotedPrice: Decimal    var unit: UnitType    var minOrderQuantity: Decimal?    var note: String?    var createdAt: Date    var updatedAt: Date    init(        id: UUID = UUID(),        supplierID: UUID,        ingredientID: UUID,        quoteDate: Date,        quotedPrice: Decimal,        unit: UnitType,        minOrderQuantity: Decimal? = nil,        note: String? = nil,        createdAt: Date = Date(),        updatedAt: Date = Date()    ) {        self.id = id        self.supplierID = supplierID        self.ingredientID = ingredientID        self.quoteDate = quoteDate        self.quotedPrice = quotedPrice        self.unit = unit        self.minOrderQuantity = minOrderQuantity        self.note = note        self.createdAt = createdAt        self.updatedAt = updatedAt    }}
```

要求：

- 不要把價差百分比存進 SupplierQuote。
- 價差、排名、狀態全部由 PriceComparisonService 即時計算。
- 若未來要快取報表，請建立 read model，不要污染原始 quote。

---

### 4.5 IngredientMarketMapping

```
@Model final class IngredientMarketMapping {    var id: UUID    var restaurantID: UUID    var ingredientID: UUID    var marketItemCode: String    var marketItemName: String    var confidence: Decimal    var source: MappingSource    var isUserConfirmed: Bool    var confirmedAt: Date?    var createdAt: Date    var updatedAt: Date    init(        id: UUID = UUID(),        restaurantID: UUID,        ingredientID: UUID,        marketItemCode: String,        marketItemName: String,        confidence: Decimal,        source: MappingSource,        isUserConfirmed: Bool = false,        confirmedAt: Date? = nil,        createdAt: Date = Date(),        updatedAt: Date = Date()    ) {        self.id = id        self.restaurantID = restaurantID        self.ingredientID = ingredientID        self.marketItemCode = marketItemCode        self.marketItemName = marketItemName        self.confidence = confidence        self.source = source        self.isUserConfirmed = isUserConfirmed        self.confirmedAt = confirmedAt        self.createdAt = createdAt        self.updatedAt = updatedAt    }}
```

用途：

- 保存「餐廳食材名稱」到「市場作物名稱」的對應。
- MVP 先用 synonym table。
- AI 只能產生候選，不得自動確認。

---

### 4.6 MarketPriceFetchLog

```
@Model final class MarketPriceFetchLog {    var id: UUID    var sourceName: String    var startedAt: Date    var finishedAt: Date?    var status: FetchStatus    var recordCount: Int    var errorMessage: String?    init(        id: UUID = UUID(),        sourceName: String,        startedAt: Date = Date(),        finishedAt: Date? = nil,        status: FetchStatus,        recordCount: Int = 0,        errorMessage: String? = nil    ) {        self.id = id        self.sourceName = sourceName        self.startedAt = startedAt        self.finishedAt = finishedAt        self.status = status        self.recordCount = recordCount        self.errorMessage = errorMessage    }}
```

用途：

- 顯示最近一次行情更新時間。
- 無網路時，讓 UI 顯示「使用上次快取行情」。
- partial import 時記錄成功筆數與錯誤訊息。

---

## 5. Enums

請建立可 Codable、CaseIterable 的 enum。  
若 SwiftData 對 enum 儲存有既有 pattern，請依專案實作調整。

```
enum UnitType: String, Codable, CaseIterable, Identifiable {    case kg    case g    case liter    case ml    case piece    case box    case catty    var id: String { rawValue }}enum IngredientCategory: String, Codable, CaseIterable, Identifiable {    case vegetable    case fruit    case meat    case seafood    case dairy    case dryGoods    case seasoning    case beverage    case other    var id: String { rawValue }}enum MappingSource: String, Codable, CaseIterable {    case exact    case synonym    case fuzzy    case aiSuggested    case userConfirmed}enum FetchStatus: String, Codable, CaseIterable {    case success    case failed    case partial}enum PriceComparisonStatus: String, Codable, CaseIterable {    case cheaperThanMarket    case nearMarket    case higherThanMarket    case missingMarketData    case unitMismatch}enum NegotiationTone: String, Codable, CaseIterable, Identifiable {    case polite    case firm    case friendly    case longTermPartner    var id: String { rawValue }}
```

請提供繁體中文 displayName extension，例如：

- kg：公斤
- g：公克
- catty：台斤
- cheaperThanMarket：低於市場
- nearMarket：接近市場
- higherThanMarket：偏高
- missingMarketData：缺少行情
- unitMismatch：單位需確認

---

## 6. DTO 與 Mapper

### 6.1 MarketPriceDTO

請建立 DTO，不要讓 API response 直接進 SwiftData model。

```
struct MarketPriceDTO: Codable {    let tradeDateRaw: String    let categoryCode: String?    let itemCode: String?    let itemName: String    let marketCode: String?    let marketName: String    let highPriceRaw: String?    let midPriceRaw: String?    let lowPriceRaw: String?    let averagePriceRaw: String    let volumeRaw: String?}
```

若實際 API 欄位為中文，請建立 CodingKeys，例如：

- 交易日期
- 種類代碼
- 作物代號
- 作物名稱
- 市場代號
- 市場名稱
- 上價
- 中價
- 下價
- 平均價
- 交易量

注意：  
實際欄位可能含有「元_公斤」或括號格式，請先查看 mock / API response，做容錯 mapping。

---

### 6.2 MarketPriceRecordInput

請建立非 SwiftData 的 input struct，讓 mapper 產生乾淨資料，再由 repository 寫入 SwiftData。

```
struct MarketPriceRecordInput: Equatable {    let categoryCode: String?    let itemCode: String    let itemName: String    let marketCode: String?    let marketName: String    let tradeDate: Date    let highPrice: Decimal?    let midPrice: Decimal?    let lowPrice: Decimal?    let averagePrice: Decimal    let volume: Decimal?    let unit: String    let sourceName: String    let fetchedAt: Date    let sourceKey: String}
```

---

### 6.3 MarketPriceMapper

```
struct MarketPriceMapper {    func map(        _ dto: MarketPriceDTO,        sourceName: String,        fetchedAt: Date    ) throws -> MarketPriceRecordInput}
```

要求：

- 支援民國日期與西元日期。
- 支援 yyyy.MM.dd、yyy.MM.dd、yyyy/MM/dd、yyyy-MM-dd 等常見格式。
- Decimal parser 需處理：
    - 空字串
    - "-"
    - nil
    - comma
    - whitespace
- averagePrice 無法解析時 throw mapping error。
- itemCode 缺漏時可用 itemName fallback，但要記錄 warning。
- sourceKey 必須 deterministic。
- unit 預設為 kg，因農產品交易行情價格單位為元/公斤。
- mapper 不得寫入資料庫。

---

## 7. Service：AgriPriceAPIClient

### 7.1 Protocol

```
protocol AgriPriceAPIClient {    func fetchMarketPrices(        from startDate: Date,        to endDate: Date,        itemName: String?,        itemCode: String?,        marketName: String?,        marketCode: String?    ) async throws -> [MarketPriceDTO]}
```

---

### 7.2 LiveAgriPriceAPIClient

請建立：

```
final class LiveAgriPriceAPIClient: AgriPriceAPIClient {    func fetchMarketPrices(        from startDate: Date,        to endDate: Date,        itemName: String?,        itemCode: String?,        marketName: String?,        marketCode: String?    ) async throws -> [MarketPriceDTO] {        // implement    }}
```

要求：

- 使用 URLSession。
- endpoint 與 query params 集中管理，不要散落在 ViewModel。
- 支援 date range。
- 支援 item name。
- 支援 item code。
- 支援 market name。
- 支援 market code。
- 若 API 本身不支援所有 filter，client 或 repository 可下載後本地 filter。
- 不要在 View 層做 filter。
- 錯誤要轉成 domain error，方便 UI 顯示。
- 不要 crash。
- 不要直接回傳 SwiftData model。

---

### 7.3 MockAgriPriceAPIClient

請建立：

```
final class MockAgriPriceAPIClient: AgriPriceAPIClient {    func fetchMarketPrices(        from startDate: Date,        to endDate: Date,        itemName: String?,        itemCode: String?,        marketName: String?,        marketCode: String?    ) async throws -> [MarketPriceDTO] {        // load from agri_price_mock.json    }}
```

要求：

- 從 Resources/agri_price_mock.json 讀取。
- 可支援本地 filter。
- 無網路 demo 必須可用。
- 測試必須可注入 mock client。

---

## 8. Service：PurchasingRepository / Cache

請建立 repository 或 cache service，避免 ViewModel 直接碰 API + SwiftData 細節。

建議 protocol：

```
protocol PurchasingRepository {    func importMarketPrices(        from startDate: Date,        to endDate: Date,        itemName: String?,        itemCode: String?,        marketName: String?,        marketCode: String?    ) async throws -> MarketPriceImportResult    func latestMarketRecords(        itemCode: String,        marketCode: String?,        asOf date: Date,        days: Int    ) throws -> [MarketPriceRecord]    func latestFetchLog(sourceName: String) throws -> MarketPriceFetchLog?}
```

```
struct MarketPriceImportResult {    let insertedCount: Int    let updatedCount: Int    let skippedCount: Int    let errorCount: Int    let startedAt: Date    let finishedAt: Date}
```

要求：

- 成功下載後寫入 SwiftData。
- sourceKey 重複時更新既有資料，不新增重複資料。
- import 開始與結束都要記錄 MarketPriceFetchLog。
- partial import 需記錄 skippedCount / errorCount。
- API failure 不可讓整個 Purchasing 模組 crash。
- 無網路時使用最近一次成功快取。
- UI 顯示：
    - 使用快取行情
    - 最後更新：yyyy/MM/dd HH:mm
- 若完全沒有快取，顯示：
    - 目前沒有行情資料，請稍後連線更新

---

## 9. Service：IngredientMarketMatcher

### 9.1 Supporting Types

```
struct MarketItem: Identifiable, Equatable {    let id: String    let itemCode: String    let itemName: String}struct IngredientMarketCandidate: Identifiable, Equatable {    let id: UUID    let itemCode: String    let itemName: String    let confidence: Decimal    let source: MappingSource    let reason: String}
```

---

### 9.2 Protocol

```
protocol IngredientMarketMatcher {    func candidates(        for ingredientName: String,        availableMarketItems: [MarketItem]    ) async -> [IngredientMarketCandidate]}
```

---

### 9.3 Matching priority

MVP matching priority：

1. Exact match
2. Synonym table match
3. Normalized contains match
4. Simple fuzzy match
5. AI candidate suggestion, optional

請建立 synonym JSON：

Resources/ingredient_synonyms_zh_TW.json

範例：

```
{  "高麗菜": ["甘藍", "包心白菜"],  "小黃瓜": ["胡瓜"],  "番茄": ["蕃茄", "小番茄"],  "青蔥": ["蔥", "珠蔥"],  "馬鈴薯": ["洋芋"],  "空心菜": ["蕹菜"],  "地瓜葉": ["甘藷葉"],  "大白菜": ["包心白"],  "白蘿蔔": ["蘿蔔"],  "紅蘿蔔": ["胡蘿蔔"]}
```

要求：

- normalize 時去除空白、全半形差異、常見符號。
- exact match confidence 可設 1.0。
- synonym match confidence 可設 0.9。
- contains match confidence 可設 0.75。
- fuzzy match confidence 可設 0.6。
- AI candidate 若實作，source 必須是 aiSuggested。
- AI candidate 不得直接 confirmed。
- 使用者確認後才建立或更新 IngredientMarketMapping。
- 使用者確認後才更新 Ingredient.linkedMarketItemCode / linkedMarketItemName。

---

## 10. Service：UnitConversionService

### 10.1 Protocol

```
protocol UnitConversionService {    func normalizePrice(        price: Decimal,        from unit: UnitType,        ingredient: Ingredient    ) throws -> NormalizedPrice}
```

```
struct NormalizedPrice: Equatable {    let price: Decimal    let unit: UnitType}
```

---

### 10.2 MVP conversion rules

請實作：

1. kg → kg：不變
2. g → kg：price * 1000
3. catty → kg：price / 0.6
4. liter → liter：不變
5. ml → liter：price * 1000
6. piece → kg：不可直接換算
7. box → kg：不可直接換算
8. piece / box 若未來有 pack size，可再擴充；MVP 不硬算

要求：

- 不可用 Double。
- 價格轉換使用 Decimal。
- piece 與 kg 不可直接比較。
- box 與 kg 不可直接比較。
- 無法換算時 throw UnitConversionError。
- UI 顯示「單位不同，需手動確認」，不要硬算。

---

## 11. Service：PriceComparisonService

### 11.1 Protocol

```
protocol PriceComparisonService {    func compare(        ingredient: Ingredient,        suppliers: [Supplier],        supplierQuotes: [SupplierQuote],        marketRecords: [MarketPriceRecord],        asOf date: Date    ) throws -> PriceComparisonResult}
```

---

### 11.2 Output types

```
struct PriceComparisonResult: Equatable {    let ingredientID: UUID    let ingredientName: String    let marketAverage7Days: Decimal?    let marketAverage30Days: Decimal?    let latestMarketAverage: Decimal?    let supplierComparisons: [SupplierPriceComparison]    let generatedAt: Date    let warnings: [String]}
```

```
struct SupplierPriceComparison: Identifiable, Equatable {    let id: UUID    let supplierID: UUID    let supplierName: String    let quoteID: UUID    let quoteDate: Date    let quotedPrice: Decimal    let quotedUnit: UnitType    let normalizedQuotedPrice: Decimal?    let normalizedUnit: UnitType?    let marketReferencePrice: Decimal?    let differenceAmount: Decimal?    let differencePercent: Decimal?    let rank: Int?    let status: PriceComparisonStatus}
```

---

### 11.3 Calculation rules

市場參考價：

- 優先使用 latestMarketAverage。
- 若 latestMarketAverage 不存在，使用 7 日均價。
- 若 7 日均價不存在，使用 30 日均價。
- 若都不存在，status = missingMarketData。

價差金額：  
normalized supplier price - market reference price

價差百分比：  
價差金額 / market reference price * 100

status：

- higherThanMarket：differencePercent > 5
- nearMarket：-5 <= differencePercent <= 5
- cheaperThanMarket：differencePercent < -5
- missingMarketData：沒有可用市場行情
- unitMismatch：單位不可比較

7 日與 30 日均價：

- 優先使用 volume-weighted average
- 若該期間所有 records 都沒有 volume，使用 arithmetic average
- 若部分 volume 缺漏，請忽略缺 volume 的 weighted 分子分母；若沒有任何 usable volume，fallback arithmetic average

volume-weighted average：  
sum(averagePrice * volume) / sum(volume)

供應商排名：

- 只排名 normalizedQuotedPrice 不為 nil 且 status 不是 missingMarketData / unitMismatch 的報價。
- 價格低者 rank 較前。
- 同價可給相同 rank，或依 quoteDate 排序；請在註解中說明採用邏輯。
- 不可比較者 rank = nil。

要求：

- 此 service 必須是 pure deterministic service。
- 不存資料庫。
- 不呼叫 API。
- 不呼叫 AI。
- 不依賴 SwiftUI。
- ViewModel 不可自行計算 differencePercent。
- 請寫完整 unit tests。

---

## 12. Service：NegotiationDraftAI

### 12.1 Protocol

```
protocol NegotiationDraftAI {    func generateDraft(context: NegotiationContext) async throws -> NegotiationDraft}
```

```
struct NegotiationContext: Equatable {    let restaurantName: String?    let supplierName: String    let ingredientName: String    let quotedPrice: Decimal    let marketAverage7Days: Decimal?    let marketAverage30Days: Decimal?    let differencePercent: Decimal?    let recentTrendSummary: String?    let tone: NegotiationTone}
```

```
struct NegotiationDraft: Equatable {    let message: String    let fallbackUsed: Bool}
```

---

### 12.2 AI prompt rules

AI 產生內容必須符合：

1. 使用繁體中文。
2. 語氣禮貌但有力。
3. 不要威脅供應商。
4. 不要捏造不存在的市場資料。
5. 若市場資料不足，明確說「目前參考資料有限」。
6. 文字長度適合貼到 LINE，約 80～180 字。
7. 不要輸出 Markdown。
8. 不要輸出表格。
9. 不要自稱 AI。
10. 不要說「根據系統」或「根據模型」。
11. 不要承諾不實採購量。
12. 不要編造其他供應商報價。

---

### 12.3 Fallback template

若 AI API unavailable，請使用 deterministic fallback template。

範例：

```
您好，想跟您確認一下這次 {ingredientName} 的報價。目前我們看到近期市場均價大約是 {marketPrice}，這次報價約高出 {differencePercent}%。我們一直希望穩定跟您配合，也想請您幫忙看看這批價格是否還有調整空間，謝謝。
```

若市場資料不足：

```
您好，想跟您確認一下這次 {ingredientName} 的報價。目前參考資料有限，但這次報價對我們的成本影響較明顯。我們一直希望穩定跟您配合，也想請您幫忙看看價格是否還有調整空間，謝謝。
```

要求：

- fallbackUsed = true。
- fallback 不可捏造市場均價。
- fallback message 也必須可直接複製使用。
- AI failure 不可影響價差報表。

---

## 13. Screens / Views

請依照既有 App navigation 整合。  
若已有 tab、sidebar 或 module launcher，請將 Purchasing 放入 RestaurantOps 內。

---

### 13.1 IngredientListView

功能：

- 顯示餐廳常用食材。
- 新增 / 編輯 / 刪除食材。
- 搜尋食材。
- 顯示食材分類與預設單位。
- 顯示是否已連結市場品項。
- 點擊進入 IngredientDetailView。

UI 狀態：

- Empty state：「尚未建立常用食材」
- 未連結市場品項 badge：「尚未對應行情」
- 已連結時顯示市場作物名稱。
- 搜尋無結果時顯示：「找不到符合的食材」

---

### 13.2 IngredientDetailView

功能：

- 顯示食材基本資料。
- 顯示目前市場品項對應。
- 可重新執行 matcher。
- 顯示候選市場品項。
- 使用者點選「確認對應」後儲存 mapping。
- 可取消或更換對應。

重要：

- AI 候選需標示「AI 建議，請確認」。
- 不可自動套用 AI 候選。
- 使用者確認後才更新 Ingredient.linkedMarketItemCode / linkedMarketItemName。
- 若沒有候選，顯示「找不到相近市場品項，請稍後更新行情或手動搜尋」。

---

### 13.3 MarketPriceTrendView

功能：

- 顯示該食材近 7 / 30 日行情。
- 顯示最高價、中價、最低價、平均價。
- 顯示資料來源。
- 顯示最後更新時間。
- 顯示是否使用快取。
- 無資料時顯示提示。

注意：

- 可使用 Swift Charts。
- 若專案不支援 Swift Charts，先用列表顯示。
- 不要在 Chart 裡計算均價。
- ViewModel 應呼叫 service / repository 取得已計算資料。

---

### 13.4 SupplierListView

功能：

- 新增 / 編輯 / 刪除供應商。
- 搜尋供應商。
- 顯示電話、LINE ID、備註。
- 進入供應商報價紀錄。
- 新增供應商時，若同餐廳下有相似名稱，UI 提示疑似重複，但不阻擋儲存。

Empty state：  
「尚未建立供應商」

---

### 13.5 SupplierQuoteEntryView

功能：

- 選擇供應商。
- 選擇食材。
- 輸入報價。
- 選擇單位。
- 輸入最低訂購量。
- 輸入備註。
- 儲存 quote。

驗證：

- quotedPrice 必須 > 0。
- quoteDate 不可為未來日期。
- supplierID 必須存在。
- ingredientID 必須存在。
- 單位若與 ingredient default unit 不同，顯示提醒但允許儲存。
- quotedPrice 請使用 Decimal parsing，避免 Double。

錯誤訊息：

- 「請輸入有效報價」
- 「報價日期不可晚於今天」
- 「請選擇供應商」
- 「請選擇食材」
- 「單位不同，儲存後比價時可能需要確認」

---

### 13.6 PriceComparisonDashboardView

功能：

- 選擇食材。
- 顯示市場最新均價。
- 顯示 7 日均價。
- 顯示 30 日均價。
- 顯示各供應商報價比較。
- 顯示價差金額與百分比。
- 顯示供應商價格排名。
- 支援多供應商比較。
- 可從某一筆比較結果進入 NegotiationAssistantView。

顯示規則：

- higherThanMarket：標示「偏高」
- nearMarket：標示「接近市場」
- cheaperThanMarket：標示「低於市場」
- missingMarketData：標示「缺少行情」
- unitMismatch：標示「單位需確認」

要求：

- 價差與百分比必須由 PriceComparisonService 提供。
- View 不得自行計算。
- Dashboard 必須在 AI 不可用時仍完整可用。
- 無行情資料時仍顯示供應商報價，但狀態為 missingMarketData。
- 單位不可比較時仍顯示報價，但狀態為 unitMismatch。

---

### 13.7 NegotiationAssistantView

功能：

- 從某一筆 SupplierPriceComparison 進入。
- 顯示 deterministic comparison summary。
- 顯示資料依據。
- 選擇語氣。
- 產生議價訊息。
- 可編輯訊息。
- 可複製訊息。
- 可使用 system share sheet 分享。
- 複製成功後顯示 toast 或 alert。

資料依據區塊至少顯示：

- 食材名稱
- 供應商名稱
- 供應商報價
- 市場參考價
- 價差百分比
- 7 日均價
- 30 日均價
- 最後行情更新時間
- 是否使用快取

要求：

- 產生前先顯示資料依據。
- AI 不可用時顯示 fallback template。
- 不要要求使用者登入 AI 才能看到價差報表。
- 產出的文字不含 Markdown 表格。
- 產出的文字可直接貼到 LINE。

---

## 14. ViewModel 要求

每個 View 請建立或使用對應 ViewModel。

ViewModel 職責：

- 管理 UI state。
- 呼叫 repository / service。
- 處理 loading / empty / error state。
- 將 service output format 成 UI display model。
- 不做核心價格計算。

不得：

- 不得在 ViewModel 手算 differencePercent。
- 不得在 ViewModel 手算 7 日 / 30 日均價。
- 不得在 ViewModel 直接呼叫 URLSession。
- 不得在 ViewModel 直接組 AI prompt，除非該專案沒有 service 層；即使如此，也應抽到 NegotiationDraftAI。

---

## 15. Offline / Cache 要求

請確保：

1. 行情下載成功後存入 SwiftData。
2. 無網路時，使用最近一次成功快取。
3. UI 顯示「使用快取行情」。
4. UI 顯示「最後更新：yyyy/MM/dd HH:mm」。
5. 若完全沒有快取，顯示「目前沒有行情資料，請稍後連線更新」。
6. API failure 不可造成整個 Purchasing 模組 crash。
7. partial import 需記錄成功筆數與錯誤筆數。
8. Fetch log 要可供 UI 讀取。
9. Mock JSON 可在無網路環境使用。

---

## 16. Mock JSON

請建立 Resources/agri_price_mock.json。

內容至少包含：

- 高麗菜 / 甘藍
- 小黃瓜 / 胡瓜
- 番茄 / 蕃茄 / 小番茄
- 青蔥 / 蔥
- 馬鈴薯 / 洋芋
- 至少 2 個市場
- 至少 30 天資料
- 至少一筆缺 volume
- 至少一筆空字串價格，用於測試 mapper 容錯
- 至少一筆 duplicate sourceKey，用於測試 upsert

範例欄位請貼近實際農產品交易行情格式：

- 交易日期
- 種類代碼
- 作物代號
- 作物名稱
- 市場代號
- 市場名稱
- 上價
- 中價
- 下價
- 平均價
- 交易量

---

## 17. 測試要求

請新增 unit tests。  
若專案已有測試架構，沿用。  
若沒有，請建立 XCTest target 或既有可用方式。

---

### 17.1 PriceComparisonServiceTests

至少包含：

1. 供應商報價 120，市場均價 100，價差 = +20%，status = higherThanMarket。
2. 供應商報價 96，市場均價 100，價差 = -4%，status = nearMarket。
3. 供應商報價 85，市場均價 100，價差 = -15%，status = cheaperThanMarket。
4. 缺少市場資料時，status = missingMarketData。
5. piece 與 kg 不可比較，status = unitMismatch。
6. 多供應商報價時，依 normalized price 排名。
7. 7 日均價正確。
8. 30 日均價正確。
9. volume 存在時使用 volume-weighted average。
10. volume 全部缺漏時使用 arithmetic average。
11. 不可比較的 supplier quote rank = nil。
12. marketReferencePrice 為 0 或 nil 時不可除以 0。

---

### 17.2 UnitConversionServiceTests

至少包含：

1. kg → kg 不變。
2. g 單價轉 kg 單價。
3. 台斤 catty 轉 kg。
4. liter → liter 不變。
5. ml 單價轉 liter 單價。
6. piece 無 pack size 時不可轉 kg。
7. box 無 conversion rule 時不可轉 kg。
8. 所有換算使用 Decimal，不出現 Double 精度問題。

---

### 17.3 IngredientMarketMatcherTests

至少包含：

1. exact match。
2. synonym match。
3. contains match。
4. fuzzy match。
5. AI candidate source = aiSuggested。
6. AI candidate 不得自動 confirmed。
7. 使用者確認後才建立 IngredientMarketMapping。
8. 使用者確認後才更新 Ingredient linkedMarketItemCode。
9. 找不到候選時回傳 empty candidates。

---

### 17.4 AgriPriceAPIClientTests / MarketPriceMapperTests

至少包含：

1. mock JSON 可正確 decode。
2. 中文欄位可正確 map。
3. 民國日期可正確 parse。
4. 西元日期可正確 parse。
5. 空字串價格不 crash。
6. averagePrice 缺漏時該筆跳過或 mapper throw，且 import 不 crash。
7. 重複 sourceKey 不重複新增。
8. date range filter 正確。
9. item name filter 正確。
10. market name filter 正確。
11. volume 缺漏可為 nil。
12. sourceKey deterministic。

---

### 17.5 NegotiationDraftAITests

至少包含：

1. AI success 回傳 message。
2. AI failure 使用 fallback template。
3. fallbackUsed = true。
4. fallback message 不可捏造市場資料。
5. 市場資料不足時，message 包含「目前參考資料有限」。
6. message 可複製使用，不含 Markdown 表格。
7. 不同 tone 可產生不同語氣或至少不同 template。
8. 不得輸出威脅供應商的內容。

---

### 17.6 ViewModel Tests

若既有專案有 ViewModel tests，請補：

1. IngredientListViewModel empty state。
2. IngredientDetailViewModel confirm mapping flow。
3. SupplierQuoteEntryViewModel validation。
4. PriceComparisonDashboardViewModel AI unavailable still shows report。
5. NegotiationAssistantViewModel AI failure uses fallback。
6. Offline cache state displays last fetch time。

---

## 18. 驗收標準

請確保以下全部通過：

1. 無網路時顯示最近一次行情快取。
2. 同一食材可比較多個供應商。
3. 價差由 PriceComparisonService 計算。
4. 7 日均價由 PriceComparisonService 計算。
5. 30 日均價由 PriceComparisonService 計算。
6. 供應商排名由 PriceComparisonService 計算。
7. AI 產生的議價訊息可複製。
8. AI 產生的議價訊息可用 system share sheet 分享。
9. AI 不可用時，仍顯示價差報表。
10. AI 不可用時，仍可產生 fallback 議價訊息。
11. AI 不得自動確認食材與市場作物的對應。
12. 單位不可比較時，不得硬算。
13. SwiftData model 不直接依賴 API DTO。
14. View 不直接呼叫 API client。
15. ViewModel 不自行計算價差百分比。
16. 所有價格計算使用 Decimal。
17. Mock JSON 可在無網路環境跑 demo。
18. Unit tests 覆蓋核心服務。
19. API failure 不會讓 Purchasing 模組 crash。
20. partial import 會記錄成功筆數與錯誤筆數。
21. duplicate sourceKey 會 update，不會 duplicate insert。
22. 使用者可手動確認或更換市場品項 mapping。
23. Dashboard 顯示資料來源與最後更新時間。
24. Dashboard 顯示是否使用快取資料。
25. Quote entry 有基本驗證，避免無效報價。

---

## 19. 非目標 / 不要做

本次 MVP 請不要做：

1. 不要獨立 App。
2. 不要做供應商自動下單。
3. 不要自動發 LINE 訊息。
4. 不要串 LINE Login。
5. 不要做付款功能。
6. 不要做完整 CRM。
7. 不要讓 AI 自動修改價格資料。
8. 不要讓 AI 自動確認市場品項 mapping。
9. 不要把 AI 產生的話術自動送出。
10. 不要為了 demo 硬算 piece / box 與 kg。
11. 不要把 calculated differencePercent 存進 SupplierQuote。
12. 不要把 API DTO 當 SwiftData model 使用。
13. 不要把 business logic 寫在 SwiftUI View 裡。

---

## 20. 建議實作順序

請依以下順序實作，避免 UI 先行造成邏輯分散。

### Phase A：資料與 deterministic core

1. 新增 enums。
2. 新增 SwiftData models。
3. 新增 DTO。
4. 新增 MarketPriceMapper。
5. 新增 AgriPriceAPIClient protocol。
6. 新增 MockAgriPriceAPIClient。
7. 新增 UnitConversionService。
8. 新增 PriceComparisonService。
9. 新增 core unit tests。

完成 Phase A 後，先跑測試。

---

### Phase B：行情匯入與快取

1. 新增 PurchasingRepository / PurchasingCacheService。
2. 實作 importMarketPrices。
3. 實作 sourceKey upsert。
4. 實作 MarketPriceFetchLog。
5. 實作 offline cache fallback。
6. 新增 mock JSON。
7. 新增 import/cache tests。

完成 Phase B 後，先跑測試。

---

### Phase C：食材與市場品項對應

1. 新增 synonym table。
2. 實作 IngredientMarketMatcher。
3. 實作 candidate ranking。
4. 實作使用者確認 mapping。
5. 新增 matcher tests。

完成 Phase C 後，先跑測試。

---

### Phase D：供應商與報價

1. 實作 Supplier CRUD。
2. 實作 SupplierQuote entry。
3. 實作 quote validation。
4. 實作多供應商比較 dashboard。
5. 確保 dashboard 使用 PriceComparisonService output。

完成 Phase D 後，先跑測試與 build。

---

### Phase E：行情趨勢與 UI 整合

1. 實作 IngredientListView。
2. 實作 IngredientDetailView。
3. 實作 MarketPriceTrendView。
4. 實作 SupplierListView。
5. 實作 SupplierQuoteEntryView。
6. 實作 PriceComparisonDashboardView。
7. 補 empty / loading / error states。
8. 補 cached data indicators。

完成 Phase E 後，先跑 build。

---

### Phase F：AI 議價助理

1. 新增 NegotiationDraftAI protocol。
2. 新增 TemplateNegotiationDraftAI fallback。
3. 若專案已有 AI provider，建立 adapter。
4. 實作 NegotiationAssistantView。
5. 實作 copy to clipboard。
6. 實作 system share sheet。
7. 新增 AI fallback tests。

完成 Phase F 後，跑完整測試與 build。

---

## 21. Validation commands

請先檢查專案使用方式，再選擇正確命令。

若是 Xcode project：

```
xcodebuild -listxcodebuild test -scheme <SchemeName> -destination 'platform=iOS Simulator,name=iPhone 15'
```

若是 Swift Package：

```
swift testswift build
```

若專案有 Makefile 或自訂 scripts，請優先使用既有命令，例如：

```
make testmake lintmake build
```

請不要假設 scheme 名稱。  
請先 inspect repo，再決定命令。  
若 build 或 test 因為既有專案問題失敗，請在 final report 說明：

- 執行了什麼命令
- 失敗原因
- 失敗是否與本次修改有關
- 建議修復方式

---

## 22. UI copy：繁體中文文案

請使用以下文案，避免簡中或英文混雜。

Common:

- 載入中…
- 發生錯誤，請稍後再試
- 儲存
- 取消
- 編輯
- 刪除
- 搜尋
- 確認
- 更新行情
- 使用快取行情
- 最後更新：

Ingredient:

- 尚未建立常用食材
- 尚未對應行情
- 已對應行情
- 確認市場品項
- AI 建議，請確認
- 找不到相近市場品項，請稍後更新行情或手動搜尋

Supplier:

- 尚未建立供應商
- 新增供應商
- 編輯供應商
- 疑似重複供應商

Quote:

- 新增報價
- 請輸入有效報價
- 報價日期不可晚於今天
- 請選擇供應商
- 請選擇食材
- 單位不同，儲存後比價時可能需要確認

Comparison:

- 市場最新均價
- 7 日均價
- 30 日均價
- 偏高
- 接近市場
- 低於市場
- 缺少行情
- 單位需確認
- 目前沒有行情資料，請稍後連線更新

Negotiation:

- 產生議價訊息
- 複製訊息
- 分享訊息
- 已複製
- 目前參考資料有限

---

## 23. Formatting rules

價格顯示：

- 新台幣整數可顯示 `$120`
- 有小數時最多 1～2 位
- Decimal format 不要經過 Double

百分比顯示：

- +20%
- -4%
- 若有小數，最多 1 位

日期顯示：

- yyyy/MM/dd
- 最後更新時間：yyyy/MM/dd HH:mm

單位顯示：

- kg：公斤
- g：公克
- liter：公升
- ml：毫升
- piece：個
- box：箱
- catty：台斤

---

## 24. Final report 格式

完成後請用以下格式回覆：

```
## Summary- ...## Files changed- ...## Main types added- Models:- Services:- ViewModels:- Views:- Tests:## Validation- Command:- Result:## Acceptance checklist- [ ] 無網路時顯示最近一次行情快取- [ ] 同一食材可比較多個供應商- [ ] 價差、均價、排名全部由 deterministic service 計算- [ ] AI 產生的議價訊息可複製 / 分享- [ ] AI 不可用時仍顯示價差報表- [ ] AI 不得自動確認食材與市場品項對應- [ ] 單位不可比較時不得硬算- [ ] 所有價格計算使用 Decimal- [ ] Mock JSON 可無網路 demo- [ ] Unit tests 覆蓋核心服務## Known limitations- ...## Manual setup needed- ...
```

請務必誠實回報未完成項目，不要聲稱未測試的功能已通過。

```
---
```