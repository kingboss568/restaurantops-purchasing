2. RestaurantOps Phase 2：餐廳進貨價格 AI 比價 + 議價助理

產品代號：RestaurantOps Purchasing  
原編號：53  
策略：併入 RestaurantOps，不獨立開 App。  
定位：幫餐廳老闆掌握食材行情，對照供應商報價，產生議價建議。

## 2.1 MVP 目標

1. 建立餐廳常用食材清單。
2. 下載 / 匯入農產品交易行情。
3. 使用者輸入供應商報價。
4. App 顯示「供應商報價 vs 市場平均價」。
5. AI 產生禮貌但有力的議價話術。

## 2.2 Data Models

```swift
@Model final class Ingredient {
    var id: UUID
    var restaurantID: UUID
    var name: String
    var category: IngredientCategory
    var unit: UnitType // kg, g, liter, piece, box
    var defaultWasteRate: Decimal
    var linkedMarketItemCode: String?
}

@Model final class MarketPriceRecord {
    var id: UUID
    var itemCode: String
    var itemName: String
    var marketName: String
    var tradeDate: Date
    var highPrice: Decimal?
    var midPrice: Decimal?
    var lowPrice: Decimal?
    var averagePrice: Decimal
    var volume: Decimal?
    var unit: String
    var sourceName: String
    var fetchedAt: Date
}

@Model final class Supplier {
    var id: UUID
    var restaurantID: UUID
    var name: String
    var phone: String?
    var lineID: String?
    var note: String?
}

@Model final class SupplierQuote {
    var id: UUID
    var supplierID: UUID
    var ingredientID: UUID
    var quoteDate: Date
    var quotedPrice: Decimal
    var unit: UnitType
    var minOrderQuantity: Decimal?
    var note: String?
}
```

## 2.3 Services

1. `AgriPriceAPIClient`
   - Protocol-first。
   - 支援 mock JSON。
   - 支援 date range、item name、market name。

2. `IngredientMarketMatcher`
   - 把使用者食材名稱對到市場作物名稱。
   - MVP 先用 synonym table。
   - AI 只做候選建議，使用者必須確認。

3. `PriceComparisonService`
   - 計算價差百分比。
   - 7 日 / 30 日平均。
   - 供應商價格排名。

4. `NegotiationDraftAI`
   - 根據價差、供應商歷史、近期行情，產生議價訊息。

## 2.4 Screens

1. `IngredientListView`
2. `IngredientDetailView`
3. `MarketPriceTrendView`
4. `SupplierListView`
5. `SupplierQuoteEntryView`
6. `PriceComparisonDashboardView`
7. `NegotiationAssistantView`

## 2.5 驗收標準

1. 無網路時顯示最近一次行情快取。
2. 同一食材可比較多個供應商。
3. 價差、均價全部用 deterministic service 計算。
4. AI 產生的議價訊息可複製到 LINE / 訊息。
5. AI 不可用時，仍顯示價差報表。